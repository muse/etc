# !/usr/bin/env bash
# Email: <mvdw at airmail dot cc>

# Directory listing related aliases.
alias ls='ls -aG'
alias ll='ls -alhog'
alias la='ls -a'

# Ask for confirmation whenever we're (re)moving files.
alias rm='rm -i'
alias mv='mv -i'

# Have sudo understand aliases.
alias sudo='sudo '
